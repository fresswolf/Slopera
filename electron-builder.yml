appId: dev.siegrist.slopera
productName: Slopera
directories:
  output: release
files:
  - out/**
  - package.json
mac:
  target: dmg
  category: public.app-category.entertainment
  # unsigned for now — set identity + notarize when an Apple dev account exists
  identity: null
win:
  target: nsis
  # full-bleed art — Windows doesn't apply the macOS rounded mask/padding
  icon: build/icon-desktop.png
linux:
  target: AppImage
  category: Network
  icon: build/icon-desktop.png
# no native modules — sqlite comes from Electron's bundled node:sqlite
npmRebuild: false
